platform :ios, '15.0'

target 'iosApp' do
  use_frameworks!

  # Shared Compose/KMP module — podspec generated by the Kotlin CocoaPods plugin.
  pod 'app', :path => '../app'

  # Native Firebase SDK that GitLive's Kotlin bindings link against at runtime.
  pod 'FirebaseAuth'
  pod 'FirebaseFirestore'
end
